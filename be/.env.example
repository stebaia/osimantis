# Copia questo file in .env e compila i valori:  cp .env.example .env
# NON committare .env (deve stare in .gitignore).

# --- Postgres ---
POSTGRES_USER=relazioni
POSTGRES_PASSWORD=changeme
POSTGRES_DB=relazioni

# --- Applicazione ---
GEMINI_API_KEY=
API_TOKEN=
